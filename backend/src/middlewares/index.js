const validateLogin = require('./validateLogin');
const validateNewAccount = require('./validateNewAccount');
const validateDeleteAccount = require('./validateDeleteAccount');
const validateUpdateAccount = require('./validateUpdateAccount');
const validateAuth = require('./validateAuth');
const validateUpdateProduct = require('./validateUpdateProduct');
const validateDeleteProduct = require('./validateDeleteProduct');
const validateNewProduct = require('./validateNewProduct');

module.exports = {
  validateLogin,
  validateNewAccount,
  validateDeleteAccount,
  validateUpdateAccount,
  validateAuth,
  validateUpdateProduct,
  validateDeleteProduct,
  validateNewProduct,
};
